# /bg

バックグラウンド実行スキル。

## 手順

このスキルが呼ばれたら、テキスト出力なしで即座に Agent ツールだけを呼ぶ。思考や説明は不要。

1. Agent ツールを `run_in_background: true` で起動する
2. テキスト出力は Agent 起動と同時に「開始」の一言だけ

引数がスキル名なら prompt は `Skill ツールで "$ARGUMENTS" を実行せよ。cwd: <cwd>` のみ。
引数が自由記述ならそのまま prompt に渡す。

## 完了後

結果を簡潔に報告する。
